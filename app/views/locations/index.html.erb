<%= render "shared/header" %>
<div class="container">
<div id="map"></div>
<script>
  // Google Maps APIの読み込みが完了したら呼び出されるコールバック関数
  function initMap() {
    // 地図を表示する位置とズームレベルを指定
    var mapOptions = {
      center: { lat: 35.6895, lng: 139.6917 }, // 東京の緯度経度
      zoom: 10
    };

    // 地図を表示する要素を取得
    var mapElement = document.getElementById('map');

    // Google Mapsオブジェクトを初期化
    var map = new google.maps.Map(mapElement, mapOptions);

    // マーカーの設定
    var marker = new google.maps.Marker({
      position: { lat: 35.6895, lng: 139.6917 },
      map: map,
      title: 'Tokyo!'
    });
  }
</script>
    <div class="reviews">
      <div class="review">
        <%= image_tag 'review-image1.jpg' %>
        <h2>レビュータイトル1</h2>
        <p>この場所のレビュー内容がここに表示されます。たとえば、素晴らしいサービスや立地の良さなど。</p>
      </div>
      <div class="review">
        <%= image_tag 'review-image2.jpg' %>
          <h2>レビュータイトル2</h2>
          <p>別のレビュー内容がここに表示されます。レビューは複数追加できます。</p>
      </div>
            <!-- 他のレビューもここに追加できます -->
    </div>
</div>
<%= render "shared/footer" %>